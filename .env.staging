# Baked into the bundle by `vite build --mode staging`.
#
# Not secrets. Both values are addresses the browser is sent to and they ship
# in every build; they live in git so it is reviewable which environment points
# where, instead of being typed into a Cloud Build trigger nobody can diff.

VITE_API_BASE_URL=https://ojt-system-be-672553132888.asia-south1.run.app
VITE_SUPABASE_URL=https://cumeeqozfrnehtjivirf.supabase.co
